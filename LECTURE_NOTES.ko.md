# Instructor Notes: XAI Intro

이 문서는 교수자 기준의 강의 운영 메모입니다. 특히 **PDP(Partial Dependence Plot)** 설명을 빠르게 잡을 수 있도록 구성했습니다.

## 1) 강의 목표

이 수업에서 학생들이 최소한 다음 질문에 답할 수 있으면 성공입니다.

1. 모델이 무엇을 예측했는가?
2. 모델이 어떤 특징을 중요하게 봤는가?
3. 한 특징이 바뀌면 예측이 평균적으로 어떻게 달라지는가?
4. 개별 사례를 설명하는 방법(LIME, SHAP)과 전체 경향을 보는 방법(PDP, feature importance)의 차이는 무엇인가?

## 2) 전체 수업 흐름 제안

### Part A. Decision Tree / XGBoost 쪽

1. Wine 데이터 소개
2. 분류 문제 설정: red vs white
3. Accuracy와 confusion matrix 소개
4. Feature importance 소개
5. 1D PDP 소개
6. 2D PDP 소개

### Part B. Surrogate / Explanation 쪽

1. LIME text
2. LIME image
3. SHAP local explanation
4. SHAP global summary

## 3) PDP를 학생에게 설명하는 핵심 문장

### 한 줄 정의

PDP는 **다른 조건은 평균적으로 그대로 둔 채, 특정 특징 하나 또는 두 개를 바꿨을 때 모델의 예측이 어떻게 달라지는지 보여주는 그래프**입니다.

### 비전공자용 직관

- 1D PDP: "설탕 함량만 바꾸면 white wine일 확률이 평균적으로 어떻게 달라질까?"
- 2D PDP: "알코올과 설탕을 함께 바꾸면 예측이 어떻게 달라질까?"

## 4) PDP를 계산하는 개념적 절차

확률론 기호부터 시작하지 말고, 먼저 **샘플 평균 관점**으로 설명하는 것이 좋습니다.

### 1D PDP의 샘플 평균 설명

예를 들어 `sugar`의 PDP를 본다고 합시다.

1. 모든 학생/와인 샘플을 그대로 둡니다.
2. 단, `sugar` 값만 1, 2, 3, ..., 20처럼 강제로 바꿔봅니다.
3. 그때마다 모델 예측을 다시 계산합니다.
4. 같은 `sugar` 값에서 나온 예측들을 평균냅니다.
5. 그 평균값을 이어 그린 것이 PDP입니다.

즉, PDP는 **"한 변수만 움직였다고 가정했을 때의 평균 예측 곡선"** 입니다.

### 2D PDP의 샘플 평균 설명

1. `alcohol`과 `sugar` 두 값을 격자처럼 바꿉니다.
2. 각 조합에서 모델 예측을 계산합니다.
3. 나머지 변수는 실제 데이터 분포에 대해 평균냅니다.
4. 결과를 색깔이나 등고선으로 그립니다.

## 5) 수식은 이렇게만 설명하면 충분함

비전공자에게는 수식을 길게 밀기보다, 아래 정도면 충분합니다.

$$
\text{PDP}(x_j) = \text{average prediction when feature } j \text{ is fixed to } x_j
$$

강의 멘트:

"어려운 적분처럼 보여도, 실제 직관은 단순합니다. 한 특징값을 고정하고, 나머지 사람들은 그대로 둔 채 평균 예측을 계산하는 것입니다."

## 6) PDP 읽는 법

### 1D PDP

- 선이 올라가면: 그 특징이 커질수록 예측도 커지는 경향
- 선이 내려가면: 그 특징이 커질수록 예측이 작아지는 경향
- 선이 평평하면: 그 특징의 영향이 작음
- 곡선이 휘면: 단순 선형관계가 아님

Wine 예시 멘트:

- `sugar` PDP가 올라가면 "설탕이 높을수록 white wine으로 분류할 가능성이 커진다"
- `alcohol` PDP가 구간별로 다르게 움직이면 "알코올의 영향은 일정하지 않다"

### 2D PDP

- 진한 색: 예측값이 큼
- 연한 색: 예측값이 작음
- 특정 코너만 진하면: 두 변수의 조합 효과가 강함

강의 멘트:

"1D는 한 변수의 평균 효과, 2D는 두 변수의 상호작용을 보여줍니다."

## 7) PDP의 가장 중요한 한계

이 부분은 반드시 강조하는 것이 좋습니다.

### 상관관계 문제

PDP는 사실상 **'이 변수를 이렇게 바꿔도 다른 변수는 그대로 둘 수 있다'** 는 가정을 깔고 있습니다.

그런데 현실 데이터에서는 변수들이 서로 연결되어 있습니다.

예시:

- 방 개수와 집 크기
- 소득과 신용점수
- 와인의 당도와 산도 일부 구간

문제점:

모델이 실제로는 한 번도 보지 못한 이상한 조합을 PDP 계산에 넣을 수 있습니다.

강의 멘트:

"PDP는 모델을 이해하는 데 매우 유용하지만, 상관된 변수에서는 가짜 조합을 만들 수 있기 때문에 지나치게 곧이곧대로 읽으면 안 됩니다."

## 8) 학생용 비유 3개

### 비유 1. 메뉴 가격표

"재료 수만 바꿨을 때 음식 가격이 평균적으로 어떻게 달라지는지 보는 그래프"라고 설명할 수 있습니다.

### 비유 2. 기상도

2D PDP는 "온도와 습도의 조합에 따라 불쾌지수가 어떻게 바뀌는지 보는 지도"처럼 설명할 수 있습니다.

### 비유 3. 실험 통제

"다른 조건은 비슷하게 두고 한 변수만 바꿔보는 사고실험"이라고 설명하면 직관적으로 받아들입니다.

## 9) 교수자용 핵심 포인트 7개

1. PDP는 **global explanation**이다.
2. 개별 사례 설명이 아니라 평균 경향 설명이다.
3. 1D PDP는 한 변수, 2D PDP는 두 변수의 조합 효과를 본다.
4. 계산 직관은 '한 변수 고정 + 나머지 평균'이다.
5. 선형, 비선형, 평탄 구간을 읽게 해야 한다.
6. 상관관계가 강한 변수에서는 해석이 왜곡될 수 있다.
7. PDP는 인과가 아니라 **모델의 반응 패턴**을 보여준다.

## 10) LIME / SHAP / PDP 비교 멘트

- Feature importance: "전체적으로 어떤 변수가 중요한가"
- PDP: "한 변수를 바꾸면 평균 예측이 어떻게 움직이는가"
- LIME: "이 개별 샘플은 왜 이렇게 분류됐는가"
- SHAP: "이 개별 예측 또는 전체 패턴에 각 변수가 얼마만큼 기여했는가"

## 11) 바로 강의에 쓸 수 있는 멘트

### PDP 도입 멘트

"중요한 변수를 아는 것만으로는 충분하지 않습니다. 중요한 다음 질문은, 그 변수가 커질 때 예측이 올라가는지 내려가는지입니다. PDP는 바로 그 질문에 답해줍니다."

### 1D PDP 해석 멘트

"이 선은 설탕 함량을 바꿨을 때 모델이 평균적으로 white wine이라고 얼마나 더 자주 판단하는지를 보여줍니다."

### 2D PDP 해석 멘트

"이 그림은 알코올과 설탕이 따로따로가 아니라 함께 작용할 때 모델이 어떻게 반응하는지를 보여줍니다."

### 한계 강조 멘트

"다만 이 그림은 모델을 이해하기 위한 사고실험이지, 현실에서 그 변수만 독립적으로 바꿀 수 있다는 뜻은 아닙니다."

## 12) 수업 직전 5분 요약

수업 직전에 이것만 다시 보면 됩니다.

1. PDP는 평균 효과를 본다.
2. 1D는 한 변수, 2D는 두 변수 조합이다.
3. 계산은 '한 변수 고정 + 전체 평균'이다.
4. 선이 오르면 양의 관계, 내리면 음의 관계다.
5. 상관관계가 강하면 해석에 주의한다.
6. PDP는 인과가 아니라 모델의 반응이다.

## 13) 참고 메모

이 노트의 PDP 설명은 scikit-learn 공식 문서, Interpretable ML Book, 그리고 correlated feature 한계 설명 자료를 바탕으로 정리했습니다.